# Frontend

The main project documentation lives in the repo root `README.md`.

This frontend is a Vite + React application used to:

- display participants and events
- collect meeting duration and step inputs
- call the backend availability API
- render slots, stale-result state, and warnings
